```text
设计移动端 AI 语音助手 App「灵语」的主聊天页。iPhone 竖屏移动端界面，深色模式为默认。整体氛围：安静、克制、高级的科技感，参考 ChatGPT 与 Perplexity 的视觉气质，避免花哨装饰。

布局（自上而下）：
1. 顶部导航栏：左侧 Logo「灵语」+ 副标题"AI 语音助手"小字；右侧一个设置齿轮图标。
2. 中部聊天消息流，占满剩余空间：
   - 用户消息：靠右对齐，蓝色渐变圆角气泡，最大宽度 80%。
   - AI 消息：靠左对齐，半透明深灰毛玻璃气泡，圆角 16px。
   - AI 思考中：三个小圆点依次跳动 + "正在思考…"文字。
   - 每条 AI 消息下方一行小操作按钮：复制 / 重新生成 / 播放语音（小图标 + 小字）。
3. 底部输入区，贴屏幕底部安全区：
   - 正中央一个大圆形麦克风按钮，直径约 64px，深色半透明玻璃质感，带 1px 浅色描边，外围有一圈柔和的呼吸光晕动画。
   - 麦克风左侧一个键盘图标（点击切换为文字输入模式），右侧一个历史记录图标。

配色：背景深空黑 #0F1115；主色科技蓝 #5B8DEF；强调色紫 #7C5BEF、青 #00D4A0；主文字 #E8EAF0；次要文字 #8A8F9C。字体用无衬线中文字体（苹方风格）。

关键交互状态（重点表现）：
- 录音中：麦克风按钮周围向外扩散的同心圆波纹，按钮上方浮现音量波形条（蓝→紫渐变），按钮下方显示计时"00:12"和"正在聆听…"，并提示"上滑取消发送"。
- 新消息到达时消息上浮淡入；AI 回复文字逐字出现。

输出整屏高保真界面设计。
```

```text
继续细化上一个屏幕中的「麦克风录音按钮」组件，做成一个完整的组件状态稿，一个画布上并排展示以下 6 种状态：
1. 待机：圆形玻璃按钮 + 静态麦克风图标 + 缓慢呼吸光晕。
2. 按下：按钮缩小到 95%，阴影内陷。
3. 录音中：按钮变为蓝紫渐变实心，周围 3 圈扩散波纹，上方 5-7 根彩色音量柱随音量高低起伏，下方显示"00:12 · 正在聆听…"。
4. 识别中：按钮内变成旋转的加载圆环 + "识别中…"。
5. 识别失败：按钮短暂变红 #FF5252 并轻微抖动，下方提示"未识别，请重试"。
6. 权限被拒：按钮灰色禁用 + 下方引导文字"需要麦克风权限"，旁边一个小按钮"去开启"。

风格与主聊天页完全一致：深色背景 #0F1115、主色 #5B8DEF、圆角、毛玻璃、60fps 流畅微动效。每格状态下方标注状态名。
```

```text
设计 AI 消息中嵌入的「工具调用卡片」，展示 AI 调用地图/天气工具的过程。移动端深色风格，与语音助手 App 整体一致（背景 #0F1115，主色 #5B8DEF，毛玻璃卡片，圆角 16px）。

卡片三段式结构：
1. 标题栏：左侧一个小图标（地图工具用放大镜+定位针，天气用云+太阳）+ 工具名（如"附近搜索"）+ 右侧一个展开/收起箭头。
2. 中间内容区（调用中）：一行状态文字"正在调用 maps_search_around…"，下方深色代码块风格参数区显示 {"keyword": "咖啡", "radius": 1000}，左侧一个小黄点脉冲动画。
3. 底部结果区（完成后）：绿色状态灯 + "已完成 · 0.8s · 返回 5 个结果"，一行摘要文字"距您最近的有星巴克、瑞幸…"，可选一个"在地图中打开"小按钮。

失败态：状态灯变红 + "调用失败" + 轻微抖动。整卡宽撑满消息气泡，深色半透明毛玻璃质感。
```

```text
设计 AI 消息中的「地图结果卡片」，用于展示附近搜索或路径规划结果。移动端深色风格，与语音助手 App 整体一致（背景 #0F1115，主色 #5B8DEF，圆角 16px）。

卡片结构：
- 上部地图区域，高度约 180px：深色地图底图，一个蓝色水滴标记表示用户当前位置，2-3 个红色水滴标记表示搜索结果，标记带小标签；地图右下角放 "+ / −" 缩放控件，左下角小字 "© 高德地图"。
- 下部摘要条：左对齐文案"找到 5 个结果 · 距您最近 320 米"，右侧一个"查看全部"文字按钮。

同时设计它的全屏版（独立屏幕）：
- 全屏地图铺满，底部有一块可上滑的半屏结果列表：每条结果一行 = 小图标 + 名称 + 地址 + 距离 + 评分星级，分隔线隔开。
- 点击列表项时地图对应标记高亮并弹出信息窗（名称 / 地址 / 电话 / 营业时间）。
- 底部固定操作栏：两个按钮「复制地址」和「用地图导航」。
```

```text
设计移动端 AI 语音助手 App「灵语」的设置页。深色模式，风格与主聊天页一致（背景 #0F1115，毛玻璃卡片，圆角 16px，主色 #5B8DEF）。整屏为分组表单，分为 4 组：

组 1「模型设置」：
- 一行选择项：LLM 厂商（阿里云百炼 / Deepseek），右侧箭头。
- 一行选择项：模型名称（qwen-turbo / qwen-plus / deepseek-chat）。
- 一行：Temperature，左侧标签，右侧滑块（0-1 范围，当前 0.7）。
- 一行：Max Tokens，数字输入框（当前 2048）。

组 2「语音设置」：
- 第一行：TTS 语音回复开关（iOS 风格 switch 控件）。
- 音色选择：2×2 卡片网格，4 张卡片（女声·温柔 / 男声·磁性 / 童声 / 粤语），每张卡片含音色名 + 小耳机图标 + "试听"小按钮，当前选中卡片有主色描边。
- 一行：语速滑块（0.5x-2.0x，当前 1.0x）。
- 一行：音调滑块（-5 到 +5，当前 0）。

组 3「工具设置」：
- 一行：高德地图 MCP 开关。
- 一行：工具白名单，多选 chip 标签（附近搜索 / 路径规划 / 天气 / 行政区划），已选的带主色填充。
- 一行：每次调用前需确认开关。

组 4「界面与隐私」：
- 一行：主题（跟随系统 / 深色 / 浅色）。
- 一行：字体大小（小 / 中 / 大，分段控件）。
- 最后一行红色危险按钮："清空所有会话历史"，点击弹出二次确认弹窗。

底部固定两个按钮：左「重置」右「保存」（主色实心）。
```

```text
设计移动端 AI 语音助手 App「灵语」的会话历史页。深色模式，风格与主聊天页一致（背景 #0F1115，毛玻璃卡片，圆角 16px，主色 #5B8DEF）。

页面结构：
- 顶部导航栏：左侧返回箭头 + 标题"会话历史"，右侧一个"新建会话"图标按钮（+）。
- 中部会话列表，卡片式：每条会话一张圆角卡片，包含首条消息摘要（单行省略）+ 下方一行小字时间（今天显示"今天 14:32"，其他显示"昨天"或具体日期），卡片间距 12px。
- 卡片左滑露出红色"删除"按钮；长按卡片弹出操作菜单（重命名 / 删除）。
- 空状态：画面居中一个柔和插画（麦克风 + 声波纹，低饱和），标题"暂无历史记录"，副标题"开启你的第一次对话"。

底部可加一个 tabBar（聊天 / 历史 两个 tab，历史 tab 高亮），图标用简洁线性图标。
```

```text
为移动端 AI 语音助手 App「灵语」设计 5 种特殊状态页，深色风格与主聊天页一致（背景 #0F1115，主色 #5B8DEF，圆角 16px），一个画布并排展示：

1. 首次进入空状态：居中插画（动态麦克风 + 声波，可画成静态），标题"开启你的第一次对话"，副标题"按住下方按钮说话，或直接输入文字"，下方 3 个圆角示例问题气泡（"今天北京天气怎么样？" / "附近 1 公里内有什么咖啡馆？" / "从国贸到大兴机场怎么走？"），气泡可点击。

2. 麦克风权限被拒：居中插画（被斜线划掉的麦克风），标题"需要麦克风权限"，说明文字"请点击右上角 ··· → 设置 → 开启麦克风权限"，下方一个主色实心按钮"我已开启，重新授权"。

3. 网络断连：屏幕顶部一条红色横幅"网络连接已断开"，消息区域整体变灰，底部麦克风按钮灰色禁用。

4. 服务异常：居中插画（服务器小图标故障感），标题"服务暂时不可用"，副标题"我们正在努力修复，请稍后重试"，下方一个按钮"重试"。

5. 加载思考中：AI 消息气泡内三个小圆点依次跳动 + "正在思考…"；超过 5 秒追加一行小字"已等待 8 秒…"。
```

```text
为移动端 AI 语音助手 App「灵语」主聊天页设计浅色模式版本。与深色版布局、组件、交互完全一致，仅切换配色：
- 背景：#F7F8FA 浅灰白；卡片：白色半透明毛玻璃（白底 70% 透明度 + 阴影）。
- 主文字：#1A1D24；次要文字：#8A8F9C。
- 主色仍是科技蓝 #5B8DEF；强调紫 #7C5BEF、青 #00D4A0 不变。
- 用户气泡仍为蓝色渐变；AI 气泡改为白色毛玻璃卡片 + 浅灰描边。
- 录音按钮：白色玻璃质感 + 浅色光晕；录音中仍为蓝紫渐变 + 扩散波纹。
保持同款：顶部 Logo「灵语」+ 设置齿轮；中央大圆形麦克风按钮；左侧键盘图标、右侧历史图标。输出整屏高保真界面。
```
